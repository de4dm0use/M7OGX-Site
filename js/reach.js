const $=id=>document.getElementById(id);const rad=x=>x*Math.PI/180;
function calc(){const a=[+$("lat1").value,+$("lon1").value],b=[+$("lat2").value,+$("lon2").value];if(!a.every(Number.isFinite)||!b.every(Number.isFinite)){$("route").textContent="Enter a target";return}const p1=rad(a[0]),p2=rad(b[0]),dlon=rad(b[1]-a[1]),dlat=rad(b[0]-a[0]);const h=Math.sin(dlat/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dlon/2)**2;const dist=6371*2*Math.asin(Math.min(1,Math.sqrt(h)));const y=Math.sin(dlon)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dlon);const bearing=(Math.atan2(y,x)*180/Math.PI+360)%360;$("route").textContent=`${dist.toFixed(0)} km path`;$("result").innerHTML=[["DISTANCE",dist.toFixed(0)+" km"],["BEARING",bearing.toFixed(1)+"°"],["MILES",(dist*0.621371).toFixed(0)+" mi"],["PATH",(dist>20000?"long path":"normal path")]].map(([l,v])=>`<div class="metric"><small>${l}</small><strong>${v}</strong></div>`).join("")}
$("calc").onclick=calc;